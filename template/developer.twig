{% extends'layout/ranking.twig' %}
{% import 'macro/steam.twig' as steam %}

{% block title %}Top 250 best Steam Developers{% endblock %}
{% block heading %}Developer 250{% endblock %}

{% block content %}
    <p>
        Top 250 best Steam Developers of all time according to gamer reviews.
    </p>
    <p>
        Developers are ranked across all games released on Steam, excluding DLC and discontinued titles.
        A small preference is given to consistent developers such that those near the top of the ranking
        will typically have more than one release. The game shown next to each developer is the one they
        are most well known for, based on our rankings.
    </p>
{% endblock %}

{% block game %}
    <span class="title">
        {{ game.rank }}.
        <a href="{{ steam.developer_url(game.owner) }}">{{- game.owner -}}</a> /
    </span>
    <span class="knownfor">
        <a href="{{ steam.app_url(game.id) }}">{{ game.name }}</a>
        {% if game.release_date %}
        <span class="date">{{ game.release_date|date('Y') }}</span>
        {% endif %}
    </span>
    {% if game.games > 1 %}
    <span class="games">+{{ game.games - 1 }} more</span>
    {% endif %}
{% endblock %}
