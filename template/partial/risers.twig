<div>
    <h2>{{ title|default('Risers') }} today</h2>
    <ol>
        {% for game in games %}
        <li>
            <a href="http://store.steampowered.com/app/{{ game.id }}/">
                <img alt="Logo" src="http://cdn.akamai.steamstatic.com/steam/apps/{{ game.id }}/capsule_sm_120.jpg"
            ></a>
            <span>
                {{ include('partial/movement.twig', {movement: game.movement}) }}
                <a href="#{{ game.rank }}">{{ game.rank }}</a>.
                <a href="http://store.steampowered.com/app/{{ game.id }}/">{{- game.name -}}</a>
            </span>
        {% endfor %}
    </ol>
</div>
